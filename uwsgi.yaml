uwsgi:
  plugins: python
  uid: nobody
  gid: nogroup
  http-socket: 0.0.0.0:8000
  chdir: /srv/bootloader
  module: bootloader.wsgi:application
  master: True
  pidfile: /tmp/bootloader-master.pid
  vacuum: True
  max-requests: 5000
  static-map: /static=/srv/bootloader/static
